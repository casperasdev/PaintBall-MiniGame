package com.bananamc.paintball.arena;

/**
 * Arena game mode. Only TEAM_LIVES is implemented in v1; the enum exists so
 * arenas store their mode for future Capture the Flag compatibility.
 */
public enum GameMode {
    TEAM_LIVES
}
